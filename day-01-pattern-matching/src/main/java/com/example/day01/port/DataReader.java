package com.example.day01.port;

public interface DataReader {
    /**
     * Reads data from a source and returns it as a String.
     *
     * @return the data read from the source
     */
    String readData();

}
